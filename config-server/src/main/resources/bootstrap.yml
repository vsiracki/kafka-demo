spring:
  application:
    name: configserver
  config:
    import: "optional:configserver:"

  cloud:
    config:
      server:
        git:
          uri: https://github.com/vsiracki/spring-cloud-config-server-repository
          clone-on-start: true
      fail-fast: true